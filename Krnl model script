--[[ 
    Cylex-Coder: Executor Hub Edition
    Inspired by: KRNL & Delta
    User: tubles93 ✨
--]]

local L_ScreenGui = Instance.new("ScreenGui")
L_ScreenGui.Name = "Tubles93_Krnl_Hub"
L_ScreenGui.Parent = game.CoreGui
L_ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- LOGO K (Bisa Digerakkan / Draggable)
local L_LogoK = Instance.new("ImageButton")
L_LogoK.Name = "LogoK"
L_LogoK.Parent = L_ScreenGui
L_LogoK.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
L_LogoK.Position = UDim2.new(0.05, 0, 0.05, 0)
L_LogoK.Size = UDim2.new(0, 50, 0, 50)
L_LogoK.Image = "rbxassetid://6031097225" -- Logo K Badass (Contoh ID)
L_LogoK.Active = true
L_LogoK.Draggable = true -- FITUR UTAMA: BISA DIGERAKKAN 🕹️

local LogoCorner = Instance.new("UICorner", L_LogoK)
LogoCorner.CornerRadius = UDim.new(1, 0) -- Biar bulat

-- MAIN EXECUTOR FRAME (KRNL STYLE)
local L_Main = Instance.new("Frame")
L_Main.Name = "MainFrame"
L_Main.Parent = L_ScreenGui
L_Main.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
L_Main.BorderSizePixel = 0
L_Main.Position = UDim2.new(0.3, 0, 0.3, 0)
L_Main.Size = UDim2.new(0, 450, 0, 280)
L_Main.Visible = false -- Awalnya sembunyi
L_Main.Active = true
L_Main.Draggable = true

local MainCorner = Instance.new("UICorner", L_Main)
MainCorner.CornerRadius = UDim.new(0, 8)

-- TOP BAR (KRNL WHITE STYLE)
local L_TopBar = Instance.new("Frame")
L_Header = L_TopBar
L_Header.Parent = L_Main
L_Header.BackgroundColor3 = Color3.fromRGB(240, 240, 240)
L_Header.Size = UDim2.new(1, 0, 0, 30)
Instance.new("UICorner", L_Header).CornerRadius = UDim.new(0, 8)

local L_Title = Instance.new("TextLabel")
L_Title.Parent = L_Header
L_Title.Text = "KRNL | Script Manager - by : tubles93"
L_Title.TextColor3 = Color3.fromRGB(0, 0, 0)
L_Title.Font = Enum.Font.SourceSansBold
L_Title.TextSize = 16
L_Title.Size = UDim2.new(1, -50, 1, 0)
L_Title.BackgroundTransparency = 1

-- EDITOR / SEARCH BOX
local L_Editor = Instance.new("TextBox")
L_Editor.Parent = L_Main
L_Editor.PlaceholderText = "-- Paste your script here or search..."
L_Editor.Text = ""
L_Editor.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
L_Editor.Position = UDim2.new(0.02, 0, 0.15, 0)
L_Editor.Size = UDim2.new(0.96, 0, 0, 180)
L_Editor.TextColor3 = Color3.fromRGB(255, 255, 255)
L_Editor.TextXAlignment = Enum.TextXAlignment.Left
L_Editor.TextYAlignment = Enum.TextYAlignment.Top
L_Editor.ClearTextOnFocus = false
L_Editor.MultiLine = true

-- BUTTONS
local L_Execute = Instance.new("TextButton")
L_Execute.Parent = L_Main
L_Execute.Text = "EXECUTE"
L_Execute.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
L_Execute.Position = UDim2.new(0.02, 0, 0.85, 0)
L_Execute.Size = UDim2.new(0, 100, 0, 30)
L_Execute.TextColor3 = Color3.fromRGB(255, 255, 255)

local L_AutoExec = Instance.new("TextButton")
L_AutoExec.Parent = L_Main
L_AutoExec.Text = "AUTO EXEC: OFF"
L_AutoExec.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
L_AutoExec.Position = UDim2.new(0.26, 0, 0.85, 0)
L_AutoExec.Size = UDim2.new(0, 120, 0, 30)
L_AutoExec.TextColor3 = Color3.fromRGB(255, 255, 255)

-- LOGIC TOGGLE UI
L_LogoK.MouseButton1Click:Connect(function()
    L_Main.Visible = not L_Main.Visible
end)

-- LOGIC EXECUTE
L_Execute.MouseButton1Click:Connect(function()
    local code = L_Editor.Text
    local success, err = pcall(function()
        loadstring(code)()
    end)
    if not success then
        warn("Execution Error: " .. err)
    end
end)

-- LOGIC AUTO EXECUTE (Simulation)
local autoExecEnabled = false
L_AutoExec.MouseButton1Click:Connect(function()
    autoExecEnabled = not autoExecEnabled
    L_AutoExec.Text = autoExecEnabled and "AUTO EXEC: ON" or "AUTO EXEC: OFF"
    L_AutoExec.TextColor3 = autoExecEnabled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 255, 255)
    
    if autoExecEnabled then
        -- Simpan ke file (hanya jalan di executor beneran)
        if writefile then
            writefile("Tubles93_AutoExec.txt", L_Editor.Text)
        end
    end
end)

-- SEARCH SCRIPT FEATURE (Placeholder Logic)
L_Editor:GetPropertyChangedSignal("Text"):Connect(function()
    -- Lu bisa tambahin API search script di sini nanti
end)

print("Cylex-Coder: KRNL Style Hub Loaded for tubles93! 💀✨")
